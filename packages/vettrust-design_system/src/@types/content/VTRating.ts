export interface VTRating {
  TestimonialText: string;
  TestimonialFrom: string;
  NetScore: number;
  TestimonialDate: string; // with format "/Date(1662021462000)/" where 1662021462000 is a timestamp
}
